apiVersion: v1
kind: Service
metadata:
  name: mongo-service
spec:
  clusterIP: None # Headless Service for direct DNS resolution
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: mongo
